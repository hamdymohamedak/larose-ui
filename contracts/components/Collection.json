{
  "name": "Collection",
  "version": "1",
  "framework": "neutral",
  "purpose": "Collection component — see composition and states for behavior.",
  "props": [
    {
      "name": "ariaLabel",
      "type": "string",
      "required": false,
      "default": "'Collection'"
    },
    {
      "name": "columns",
      "type": "number",
      "required": false,
      "default": "4"
    },
    {
      "name": "items",
      "type": "CollectionItem[]",
      "required": true
    },
    {
      "name": "layout",
      "type": "CollectionLayout",
      "required": false,
      "default": "'grid'"
    },
    {
      "name": "selectedId",
      "type": "string",
      "required": false
    }
  ],
  "events": [
    {
      "name": "select",
      "payload": "(item: CollectionItem) => void"
    }
  ],
  "states": [
    "default"
  ],
  "defaults": {
    "ariaLabel": "'Collection'",
    "columns": "4",
    "layout": "'grid'"
  },
  "accessibility": {
    "requirements": [
      "Supports ARIA attributes via standard HTML element props."
    ],
    "aria": []
  },
  "keyboard": {
    "behavior": [
      "keyboard-accessible"
    ]
  }
}
