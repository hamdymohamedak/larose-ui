{
  "name": "SquareButton",
  "version": "1",
  "framework": "neutral",
  "purpose": "SquareButton component — see composition and states for behavior.",
  "props": [
    {
      "name": "icon",
      "type": "Node",
      "required": true,
      "description": "Symbol or icon — square buttons use icons, not text labels."
    },
    {
      "name": "pressed",
      "type": "boolean",
      "required": false,
      "default": "false",
      "description": "Toggle or pop-up behavior is handled by the parent; appearance only."
    },
    {
      "name": "tooltip",
      "type": "string",
      "required": false
    }
  ],
  "events": [],
  "states": [
    "default"
  ],
  "defaults": {
    "pressed": "false"
  },
  "accessibility": {
    "requirements": [
      "Supports ARIA attributes via standard HTML element props.",
      "Keyboard accessible; disabled and loading states must be announced."
    ],
    "aria": []
  },
  "keyboard": {
    "behavior": [
      "keyboard-accessible"
    ]
  }
}
