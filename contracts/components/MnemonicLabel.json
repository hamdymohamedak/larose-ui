{
  "name": "MnemonicLabel",
  "version": "1",
  "framework": "neutral",
  "purpose": "MnemonicLabel component — see composition and states for behavior.",
  "props": [
    {
      "name": "label",
      "type": "string",
      "required": true
    },
    {
      "name": "mnemonic",
      "type": "string",
      "required": false
    },
    {
      "name": "showAccessKey",
      "type": "boolean",
      "required": false,
      "default": "false",
      "description": "When true, underline the access key character."
    }
  ],
  "events": [],
  "states": [
    "default"
  ],
  "defaults": {
    "showAccessKey": "false"
  },
  "accessibility": {
    "requirements": [
      "Supports ARIA attributes via standard HTML element props."
    ],
    "aria": []
  },
  "keyboard": {
    "behavior": [
      "not-focusable"
    ]
  }
}
