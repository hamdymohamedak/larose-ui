{
  "name": "TabView",
  "version": "1",
  "framework": "neutral",
  "purpose": "macOS-style tab view with closable tabs and overflow handling.",
  "composition": "Use for document-style interfaces with multiple open views.",
  "interactions": [
    "TabView",
    "TabViewList",
    "TabViewTab",
    "TabViewPanel"
  ],
  "props": [
    {
      "name": "'aria-label'",
      "type": "string",
      "required": false
    },
    {
      "name": "defaultValue",
      "type": "string",
      "required": false,
      "default": "''"
    },
    {
      "name": "inset",
      "type": "boolean",
      "required": false,
      "default": "true"
    },
    {
      "name": "showTabs",
      "type": "boolean",
      "required": false,
      "default": "true"
    },
    {
      "name": "value",
      "type": "string",
      "required": false
    },
    {
      "name": "variant",
      "type": "TabViewVariant",
      "required": false,
      "default": "'bordered'"
    }
  ],
  "events": [
    {
      "name": "valueChange",
      "payload": "(value: string) => void"
    }
  ],
  "slots": [
    "default"
  ],
  "states": [
    "default",
    "active tab"
  ],
  "defaults": {
    "defaultValue": "''",
    "inset": "true",
    "showTabs": "true",
    "variant": "'bordered'"
  },
  "accessibility": {
    "requirements": [
      "Supports ARIA attributes via standard HTML element props."
    ],
    "aria": []
  },
  "keyboard": {
    "behavior": [
      "keyboard-accessible"
    ]
  },
  "uncontrolled": [
    "value"
  ]
}
