{
  "name": "UnsavedIndicator",
  "version": "1",
  "framework": "neutral",
  "purpose": "UnsavedIndicator component — see composition and states for behavior.",
  "props": [
    {
      "name": "autosaveEnabled",
      "type": "boolean",
      "required": false,
      "default": "true",
      "description": "When false, show the unsaved dot (manual save mode)."
    },
    {
      "name": "edited",
      "type": "boolean",
      "required": false,
      "default": "false"
    },
    {
      "name": "title",
      "type": "string",
      "required": true
    }
  ],
  "events": [],
  "states": [
    "default"
  ],
  "defaults": {
    "autosaveEnabled": "true",
    "edited": "false"
  },
  "accessibility": {
    "requirements": [
      "Supports ARIA attributes via standard HTML element props."
    ],
    "aria": []
  },
  "keyboard": {
    "behavior": [
      "not-focusable"
    ]
  }
}
