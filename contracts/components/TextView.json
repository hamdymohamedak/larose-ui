{
  "name": "TextView",
  "version": "1",
  "framework": "neutral",
  "purpose": "TextView component — see composition and states for behavior.",
  "props": [
    {
      "name": "editable",
      "type": "boolean",
      "required": false,
      "default": "false",
      "description": "When true, shows a keyboard-capable editor."
    },
    {
      "name": "maxHeight",
      "type": "string",
      "required": false
    },
    {
      "name": "selectable",
      "type": "boolean",
      "required": false,
      "default": "true",
      "description": "Allows selecting/copying useful text in read-only mode."
    },
    {
      "name": "typographyRole",
      "type": "TypographyRole",
      "required": false,
      "default": "'body'"
    }
  ],
  "events": [],
  "states": [
    "default"
  ],
  "defaults": {
    "editable": "false",
    "selectable": "true",
    "typographyRole": "'body'"
  },
  "accessibility": {
    "requirements": [
      "Supports ARIA attributes via standard HTML element props."
    ],
    "aria": []
  },
  "keyboard": {
    "behavior": [
      "not-focusable"
    ]
  }
}
