{
  "name": "Presence",
  "version": "1",
  "framework": "neutral",
  "purpose": "Presence component — see composition and states for behavior.",
  "props": [
    {
      "name": "immediate",
      "type": "boolean",
      "required": false,
      "default": "false",
      "description": "When true, skip exit animation and unmount immediately."
    },
    {
      "name": "placement",
      "type": "string",
      "required": false,
      "description": "Placement hint for spatial variants (toast, popover)."
    },
    {
      "name": "present",
      "type": "boolean",
      "required": true
    },
    {
      "name": "variant",
      "type": "MotionVariant",
      "required": false,
      "default": "'fade'"
    }
  ],
  "events": [
    {
      "name": "exitComplete",
      "payload": "() => void"
    }
  ],
  "states": [
    "default"
  ],
  "defaults": {
    "immediate": "false",
    "variant": "'fade'"
  },
  "accessibility": {
    "requirements": [
      "Supports ARIA attributes via standard HTML element props."
    ],
    "aria": []
  },
  "keyboard": {
    "behavior": [
      "keyboard-accessible"
    ]
  }
}
