{
  "name": "ImageWell",
  "version": "1",
  "framework": "neutral",
  "purpose": "ImageWell component — see composition and states for behavior.",
  "props": [
    {
      "name": "accept",
      "type": "string",
      "required": false,
      "default": "'image/*'"
    },
    {
      "name": "disabled",
      "type": "boolean",
      "required": false,
      "default": "false"
    },
    {
      "name": "label",
      "type": "string",
      "required": false,
      "default": "'Image'"
    },
    {
      "name": "placeholder",
      "type": "string",
      "required": false,
      "default": "'Drop an image, paste, or click to choose'"
    },
    {
      "name": "src",
      "type": "string",
      "required": false
    }
  ],
  "events": [
    {
      "name": "imageChange",
      "payload": "(file: File | null, previewUrl: string | null) => void"
    }
  ],
  "states": [
    "default",
    "disabled"
  ],
  "defaults": {
    "accept": "'image/*'",
    "disabled": "false",
    "label": "'Image'",
    "placeholder": "'Drop an image, paste, or click to choose'"
  },
  "accessibility": {
    "requirements": [
      "Supports ARIA attributes via standard HTML element props."
    ],
    "aria": []
  },
  "keyboard": {
    "behavior": [
      "not-focusable"
    ]
  }
}
