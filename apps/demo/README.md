# @larose-ui/demo (legacy)

Prefer **`@larose-ui/sandbox-react`** for day-to-day React visual QA:

```bash
pnpm sandbox:react
```

This app remains a small production-style React sample (employees + data/forms). Root `pnpm demo` now launches the React sandbox.
