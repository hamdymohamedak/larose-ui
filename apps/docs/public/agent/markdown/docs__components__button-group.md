# ButtonGroup

Category: Actions

## Props
- `'aria-label'` (string)
- `fullWidth` (boolean) — Full-width buttons for watchOS-style primary actions.
- `orientation` ('horizontal' | 'vertical') — Horizontal row (default) or vertical stack for visionOS guidance.

Metadata: /components/button-group.json
