# ActivityShareButton

Category: Actions

## Props
- `activities` (ActivityItem[])
- `excludedActivityIds` (string[])
- `label` (string)
- `onActivitySelect` ((activity: ActivityItem) => void)
- `presentation` ('sheet' | 'popover' | 'auto') — Uses popover on wide viewports and sheet on compact widths when auto.

Metadata: /components/activity-share-button.json
