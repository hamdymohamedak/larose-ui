# WheelPicker

Category: Forms

## Props
- `'aria-label'` (string)
- `columns` (PickerColumn[])
- `compact` (boolean) — Compact popover presentation — wheels fill the panel edge-to-edge.
- `disabled` (boolean)
- `inline` (boolean) — Removes chrome for embedding in inline layouts.
- `value` (PickerValue)

Metadata: /components/wheel-picker.json
