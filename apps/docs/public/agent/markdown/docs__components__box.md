# Box

Category: Layout

## Props
- `'aria-label'` (string)
- `padding` ('sm' | 'md' | 'lg')
- `settingsStyle` (boolean) — macOS settings panes append a colon to the title.
- `titlePosition` (BoxTitlePosition)
- `variant` (BoxVariant)

Metadata: /components/box.json
