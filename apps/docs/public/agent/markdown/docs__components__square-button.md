# SquareButton

Category: Actions

## Props
- `icon` (ReactNode) — Symbol or icon — square buttons use icons, not text labels.
- `pressed` (boolean) — Toggle or pop-up behavior is handled by the parent; appearance only.
- `tooltip` (string)

Metadata: /components/square-button.json
