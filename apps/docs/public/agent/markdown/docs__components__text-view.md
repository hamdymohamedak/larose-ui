# TextView

Category: Content

## Props
- `editable` (boolean) — When true, shows a keyboard-capable editor.
- `maxHeight` (string)
- `selectable` (boolean) — Allows selecting/copying useful text in read-only mode.
- `typographyRole` (TypographyRole)

Metadata: /components/text-view.json
