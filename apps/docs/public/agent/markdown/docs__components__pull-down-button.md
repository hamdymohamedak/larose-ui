# PullDownButton

Category: Navigation

## Props
- `destructiveConfirmation` (PullDownDestructiveConfirmation)
- `disabled` (boolean)
- `entries` (PullDownEntry[])
- `icon` (ReactNode)
- `label` (string) — Button label (required unless variant is "more").
- `longPress` (boolean) — Safari-style touch-and-hold to reveal the menu.
- `menuTitle` (string) — Optional menu title — use only when it adds meaning.
- `onAction` ((entry: MenuItemConfig) => void)
- `variant` (PullDownButtonVariant)

Metadata: /components/pull-down-button.json
