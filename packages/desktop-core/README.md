# @larose-ui/desktop-core

Framework-agnostic desktop host utilities for laRose UI:

- Host registration (`registerHost`)
- Native accelerator conversion (Electron / Tauri)
- MenuBar → native menu mapping
- Optional window chrome CSS variables

See [docs/ecosystem/DESKTOP.md](../../docs/ecosystem/DESKTOP.md).
