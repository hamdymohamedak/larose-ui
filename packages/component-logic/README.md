# `@larose-ui/component-logic`

Shared framework-agnostic component logic (utils, HIG helpers, domain transforms).

React, Vue, and Svelte adapters re-export from this package instead of duplicating `utils.ts`.
